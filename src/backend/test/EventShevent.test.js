const { ethers } = require("hardhat");
const { expect } = require('chai');
const toWei = (num) => ethers.utils.parseEther(num.toString())
const fromWei = (num) => ethers.utils.formatEther(num);

describe("Event Shevent",function(){

    
})